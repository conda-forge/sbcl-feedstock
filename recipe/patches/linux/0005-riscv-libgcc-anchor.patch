--- src/runtime/riscv-linux-os.c
+++ src/runtime/riscv-linux-os.c
@@ -95,6 +95,10 @@
 // observable to the linker. Any one symbol suffices to resolve all of them.
 #include <math.h>
 const long libm_anchor = (long)acos;
+/* Anchor libgcc_s.so.1 into DT_NEEDED; defeats --as-needed since zig
+   does not support -Wl,--no-as-needed (see 0004 for the same pattern with libm). */
+extern void _Unwind_Resume(void);
+void * const libgcc_anchor __attribute__((used)) = (void *) _Unwind_Resume;

 #include <sys/types.h>
 #include <sys/stat.h>
